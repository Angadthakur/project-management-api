services:

  api:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env.docker

  mongodb:
    image: mongo:8
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db  

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  mongodb_data:  